{
  "name": "icp_azle_react",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/backend",
    "src/frontend"
  ],
  "dependencies": {
    "@dfinity/agent": "^2.3.0",
    "@tailwindcss/vite": "^4.0.15",
    "icp-llm-client": "^1.1.0",
    "tailwindcss": "^4.0.15"
  }
}
